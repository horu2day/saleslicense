
📚 검색 쿼리: ZoomExtents

============================================================
HmEG 라이브러리 문서에서 찾은 **ZoomExtents**에 대한 정보는 다음과 같습니다.

### 1. 관련 클래스/메서드 이름

**메서드 (Method):**

*   `ZoomExtents(animationTime)`
    *   **클래스:** `HmEG.EGViewport`, `HmEG.CameraController`
*   `ZoomExtents(bounds, animationTime, autoNearFar)`
    *   **클래스:** `HmEG.EGViewport`
*   `ZoomExtents(bounds, animationTime)`
    *   **클래스:** `HmEG.EGViewport`
*   `ZoomExtents(viewport, animationTime)` (확장 메서드)
    *   **클래스:** `HmEG.ViewportExtensions`
*   `ZoomExtents(viewport, bounds, animationTime, autoNearFar)` (확장 메서드)
    *   **클래스:** `HmEG.ViewportExtensions`
*   `ZoomExtents(viewport, center, radius, near, far, animationTime)` (확장 메서드)
    *   **클래스:** `HmEG.ViewportExtensions`

**핸들러 (Handler):**

*   `ZoomExtentsHandler(sender, e)`
    *   **클래스:** `HmEG.EGViewport`, `HmEG.CameraController` (줌 범위 명령어 처리)

### 2. 사용법 설명

*   **`ZoomExtents(animationTime)` (모델 범위로 줌):**
    *   모델의 경계(extents)까지 줌을 수행합니다.
    *   `animationTime` 매개변수는 애니메이션 효과를 위한 시간을 지정합니다 (밀리초 단위).
*   **`ZoomExtents(bounds, animationTime, autoNearFar)` (특정 Bounding Box로 줌):**
    *   지정된 바운딩 박스(`bounds`) 영역에 맞게 줌을 수행합니다.
    *   `autoNearFar` 매개변수는 near/far 평면을 바운딩에 맞게 자동으로 조정할지 여부를 결정합니다.
*   **`ZoomExtents(viewport, bounds, animationTime, autoNearFar)` (ViewportExtensions):**
    *   지정된 바운딩 박스의 경계까지 줌을 수행합니다.
*   **`ZoomExtents(viewport, center, radius, near, far, animationTime)` (Bounding Sphere로 줌):**
    *   지정된 바운딩 구(sphere)의 경계까지 줌을 수행합니다 (구의 중심(`center`)과 반지름(`radius`) 사용).

### 3. C# 코드 예제

문서에 명시적인 C# 코드 예제는 제공되지 않았습니다. 다음은 일반적인 사용 시나리오를 가정한 개념적 코드 예제입니다.

```csharp
// EGViewport 인스턴스가 있다고 가정
HmEG.EGViewport myViewport = new HmEG.EGViewport();
double animationDuration = 500.0; // 500 밀리초 애니메이션

// 1. 모델 전체 범위로 줌 (애니메이션 적용)
myViewport.ZoomExtents(animationDuration);

// 2. 특정 BoundingBox로 줌
// HmEG.HmBoundingBox 또는 System.Windows.Media.Media3D.Rect3D 사용 가능
// 가상의 바운딩 박스 생성
HmEG.HmBoundingBox myBounds = new HmEG.HmBoundingBox(new HmGeometry.HmPoint3D(-10, -10, -10), new HmGeometry.HmPoint3D(10, 10, 10));

// HmEG.HmBoundingBox를 사용하여 줌 (near/far 자동 조정)
bool adjustNearFar = true;
myViewport.ZoomExtents(myBounds, animationDuration, adjustNearFar); 

// 3. ViewportExtensions 사용 (EGViewport 객체를 첫 번째 인자로 전달)
// 이 예제에서는 1번과 동일한 효과를 냄
HmEG.ViewportExtensions.ZoomExtents(myViewport, animationDuration); 
```

### 4. 관련 네임스페이스

*   `HmEG`
*   `HmEG.ViewportExtensions`
*   `HmEG.CameraController`
*   **보조 네임스페이스:**
    *   `HmGeometry` (바운딩 박스(`HmBoundingBox`), 포인트(`HmPoint3D`) 사용 시)

### 5. 참고 문서 섹션

*   `M-HmEG-EGViewport-ZoomExtents-System-Double-` (모델 전체 줌)
*   `M-HmEG-EGViewport-ZoomExtents-System-Windows-Media-Media3D-Rect3D,System-Double,System-Boolean-` (Rect3D Bounding Box 줌)
*   `M-HmEG-ViewportExtensions-ZoomExtents-HmEG-EGViewport,System-Double-` (ViewportExtensions를 통한 줌)
*   `M-HmEG-EGViewport-ZoomExtentsHandler-System-Object,System-Windows-Input-ExecutedRoutedEventArgs-` (명령어 핸들러)
*   `M-HmEG-CameraController-ZoomExtents-System-Double-` (CameraController 줌)
============================================================

📌 출처:
  - unknown
  - unknown
  - unknown
  - unknown
  - unknown
  - unknown
  - unknown
  - unknown
  - unknown
  - unknown
